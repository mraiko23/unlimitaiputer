services:
  - type: web
    name: puter-ai-api
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install && npx puppeteer-real-browser-install@
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DISPLAY
        value: ":99"


